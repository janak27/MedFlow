# MedFlow 🏥

MedFlow is a **backend microservices project** built using **Java and Spring Boot**, focused on designing a **secure and scalable foundation** for pharmacy and healthcare operations.

At its current stage, the repository contains the **User Service**, which handles authentication, authorization, and user management. The project will be expanded gradually with additional services.

---

## 🛠 Tech Stack

* **Java 17**
* **Spring Boot**
* **Spring Security + JWT**
* **Spring Data JPA / Hibernate**
* **PostgreSQL**
* **Maven**
